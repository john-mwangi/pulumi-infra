name: hetzner
description: A stack for spinning Hetzner infrastructure
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
# dependencies:
#   - requirements.txt
